[project]
name = "fleet-telemetry-platform"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Hippythalamus" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "pytest>=9.0.3",
    "ruff>=0.15.13",
]
